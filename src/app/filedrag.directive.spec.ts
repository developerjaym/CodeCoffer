import { FiledragDirective } from './filedrag.directive';

describe('FiledragDirective', () => {
  it('should create an instance', () => {
    const directive = new FiledragDirective();
    expect(directive).toBeTruthy();
  });
});
